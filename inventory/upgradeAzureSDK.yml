- name: The upgrade azure script
  hosts: localhost
  gather_facts: no
  tasks:
    - name: Upgrade azure
      shell: pip -t install azure==2.0.0rc6 --upgrade || true
    - name: Show azure version
      shell: pip show azure || true      
